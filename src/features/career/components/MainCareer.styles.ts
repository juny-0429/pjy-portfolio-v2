import { css } from '@emotion/react';

export const mainCareerCss = {
  wrapper: css`
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 100px;
    padding: 74px 0 0;
  `,
};
